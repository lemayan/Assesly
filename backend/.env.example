DATABASE_URL="postgresql://user:password@host:5432/dbname"

# JWT secret for signing tokens (IMPORTANT: Change this in production!)
JWT_SECRET="change_me_in_production"

# CORS origins (comma-separated) - Update with your Railway frontend URL
CORS_ORIGINS="http://localhost:5173,http://localhost:3000,https://your-frontend.up.railway.app"

# Node environment
NODE_ENV="production"
